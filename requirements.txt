<<<<<<< HEAD
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
pydantic-settings==2.3.4
python-multipart==0.0.9

# Database
sqlalchemy==2.0.31
alembic==1.13.2
psycopg2-binary==2.9.9

# Configuration
python-dotenv==1.0.1

# Observability
prometheus-client==0.20.0
python-json-logger==2.0.7

=======
# ---------------------------------------------------------------------------
# Core — minimum secure versions noted inline
# ---------------------------------------------------------------------------

# fastapi: ≥0.111.1 (0.111.0 had no critical CVEs; pinned to latest stable)
fastapi==0.115.12

# uvicorn: ≥0.30.1 — no known CVEs at this range; keep [standard] for
# production-grade HTTP/1.1 + WebSocket support via httptools & websockets
uvicorn[standard]==0.34.2

# pydantic v2: ≥2.7.2 fixes a ReDoS in email-validator integration
pydantic==2.11.3

# python-multipart: ≥0.0.10 fixes CVE-2024-24762 (ReDoS in multipart parser)
python-multipart==0.0.20

# ---------------------------------------------------------------------------
>>>>>>> origin/main
# Dev / testing
# ---------------------------------------------------------------------------
pytest==8.3.5
pytest-asyncio==0.26.0

# httpx: ≥0.27.0 — no known CVEs; pinned to latest stable
httpx==0.28.1

# ---------------------------------------------------------------------------
# Linting
# ---------------------------------------------------------------------------
ruff==0.11.6
